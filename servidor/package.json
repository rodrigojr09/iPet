{
	"name": "servidor",
	"version": "1.0.0",
	"main": "dist/index.js",
	"license": "MIT",
	"scripts": {
		"dev": "yarn build && yarn start",
		"build": "tsc",
		"start": "node .",
		"prisma": "prisma generate"
	},
	"dependencies": {
		"@fastify/cors": "^11.1.0",
		"@prisma/client": "^6.14.0",
		"bcrypt": "^6.0.0",
		"fastify": "^5.5.0",
		"moment-timezone": "^0.6.0"
	},
	"devDependencies": {
		"@types/bcrypt": "^6.0.0",
		"@types/node": "^24.2.1",
		"prisma": "^6.14.0",
		"typescript": "^5.9.2"
	}
}
